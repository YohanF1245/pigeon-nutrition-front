version: '3.8'

services:
  frontend:
    build: .
    ports:
      - "3001:3000"
    environment:
      - NODE_ENV=development
      - VITE_API_URL=http://localhost:3000/api
      - ORIGIN=http://localhost:3000
    volumes:
      - .:/app
      - /app/node_modules
    command: npm run dev
